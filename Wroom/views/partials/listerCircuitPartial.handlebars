<table class="tabpayscir">
	{{#each listeCircuits}}
		<tr>
         <td class="flexTab">
            <img src="/image/pays/{{this.payadrdrap}}" class="imgTab" alt="drapeau du pays" />
            <a href="/detailCircuit/{{this.cirnum}}"> {{this.cirnom}}</a>
         </td>
      </tr>
   {{/each}}
</table>
<br class="myboth" />

