<table class="tabpayscir">
	{{#each listeGP}}
		<tr>
         <td class="flexTab">
            <img src="/image/pays/{{this.payadrdrap}}" class="imgTab" alt="drapeau du pays" />
            <a href="/detailGP/{{this.gpnum}}"> {{this.gpnom}}</a>
         </td>
      </tr>
   {{/each}}
</table>
<br class="myboth" />

